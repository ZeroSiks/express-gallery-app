doctype html
html(lang='en')
  include ../common/head.pug
  +head("Sign up to Starter MVP")
  body
    header
      include ../common/navbar.pug
      +navbar(user)
      .container
        .row
          .col-sm-12.offset-md-2.col-md-8
            .jumbotron
              h2.text-center Sign up
              .row
                .col-sm-12.col-md-12
                  form(action='/signup', method='POST')
                    div.formgroup
                      label.small.mb-1.mt-3 Mailu Addressu:
                      input(placeholder='anata wo mailu addressu onegai', required=true, name='email', type='email', value=formData.email).form-control
                      if (errors && errors.email)
                        p.small.text-danger #{errors.email}
                      label.small.mb-1.mt-3 Passuwordo:
                      input(placeholder='atarashii passucode sakusei suru',required=true, name='password', type='password', value='').form-control
                      if (errors && errors.password)
                        p.small.text-danger #{errors.password}
                      button.mt-3.btn.btn-primary(type='submit') Sign up
                      p.mt-2.mb-0
                        | accountu o iruka? #[a(href="/login") koko ga loggu in].

    include ../common/footer.pug
    +footer()