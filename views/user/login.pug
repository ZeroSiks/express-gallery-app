doctype html
html(lang='en')
  include ../common/head.pug
  +head("Login to Starter MVP")
  body
    header
      include ../common/navbar.pug
      +navbar(user)
      .container
        .row
          .col-sm-12.offset-md-2.col-md-8
            .jumbotron
              h2.text-center Login
              .row
                .col-sm-12.col-md-12.col-xs-12
                  form(action='/login', method='POST')
                    div.formgroup
                      label.small.mb-1.mt-3 Mailu Addressu:
                      input(placeholder='anata wo mailu addressu onegai', required=true, name='email', type='email', value='').form-control
                      label.small.mb-1.mt-3 Passuwordu:
                      input(placeholder='anata ga passucode o enter kudasai',required=true, name='password', type='password', value='').form-control
                      button.mt-3.btn.btn-primary(type='submit') Loggu inn
                      p.mt-2.mb-0
                        | passucode o wasuredesuka? #[a(href="#") koko ge resetto].
                      p.m-0
                        | accountu o nai desuka? #[a(href="/signup") sign up kudasai].

    include ../common/footer.pug
    +footer()